const {app,BrowserWindow,WebContentsView,ipcMain,session,dialog,Menu}=require('electron');const path=require('path'),fs=require('fs');let win,activeId=null,nextId=1;const tabs=new Map();const HOME='file://'+path.join(__dirname,'renderer','home.html');const DATA=path.join(app.getPath('userData'),'nova-data.json');let store={bookmarks:[],history:[],extensions:[],downloads:[]};function loadStore(){try{if(fs.existsSync(DATA))store={...store,...JSON.parse(fs.readFileSync(DATA,'utf8'))}}catch(e){}}function saveStore(){try{fs.mkdirSync(path.dirname(DATA),{recursive:true});fs.writeFileSync(DATA,JSON.stringify(store,null,2))}catch(e){}}function addHistory(url,title){if(!/^https?:/i.test(url))return;store.history=[{url,title:title||url,time:Date.now()},...store.history.filter(x=>x.url!==url)].slice(0,500);saveStore()}function makeTab(url=HOME,privateMode=false){const id=nextId++;const ses=privateMode?session.fromPartition('incognito-'+id,{cache:false}):session.defaultSession;const view=new WebContentsView({webPreferences:{preload:path.join(__dirname,'preload.js'),contextIsolation:true,sandbox:true,session:ses}});tabs.set(id,{id,view,privateMode});view.webContents.setWindowOpenHandler(({url})=>{const n=makeTab(url,privateMode);showTab(n);return{action:'deny'}});const state=()=>sendState(id);view.webContents.on('did-navigate',()=>{if(!privateMode)addHistory(view.webContents.getURL(),view.webContents.getTitle());state()});view.webContents.on('did-navigate-in-page',state);view.webContents.on('page-title-updated',state);view.webContents.on('did-finish-load',state);view.webContents.on('will-download',(_,item)=>{const savePath=path.join(app.getPath('downloads'),item.getFilename());item.setSavePath(savePath);item.once('done',(_,s)=>{if(s==='completed'){store.downloads=[{name:item.getFilename(),path:savePath,time:Date.now()},...(store.downloads||[])].slice(0,200);saveStore();win.webContents.send('download-done',{name:item.getFilename(),path:savePath})}})});view.webContents.on('context-menu',(_,p)=>Menu.buildFromTemplate([{label:'Back',enabled:view.webContents.canGoBack(),click:()=>view.webContents.goBack()},{label:'Forward',enabled:view.webContents.canGoForward(),click:()=>view.webContents.goForward()},{type:'separator'},{label:'Reload',click:()=>view.webContents.reload()},{label:'Copy',role:'copy',enabled:!!p.selectionText},{label:'Inspect',click:()=>view.webContents.inspectElement(p.x,p.y)}]).popup({window:win}));view.webContents.loadURL(url);return id}function sendState(id){const t=tabs.get(id);if(!t||!win||win.isDestroyed())return;win.webContents.send('tab-state',{id,title:(t.privateMode?'🕶️ ':'')+(t.view.webContents.getTitle()||'New Tab'),url:t.view.webContents.getURL(),privateMode:t.privateMode,active:id===activeId})}function showTab(id){const t=tabs.get(id);if(!t)return;if(activeId&&tabs.has(activeId))win.contentView.removeChildView(tabs.get(activeId).view);activeId=id;win.contentView.addChildView(t.view);layout();tabs.forEach((_,k)=>sendState(k))}function closeTab(id){const t=tabs.get(id);if(!t)return;const was=activeId===id;if(was)win.contentView.removeChildView(t.view);t.view.webContents.close();tabs.delete(id);if(!tabs.size)showTab(makeTab());else if(was)showTab([...tabs.keys()][0]);win.webContents.send('tabs-reset')}function layout(){if(!win||!activeId||!tabs.has(activeId))return;const b=win.getContentBounds();tabs.get(activeId).view.setBounds({x:0,y:112,width:b.width,height:Math.max(0,b.height-112)})}function createWindow(){win=new BrowserWindow({width:1400,height:900,minWidth:900,minHeight:600,title:'Nova Browser',webPreferences:{preload:path.join(__dirname,'preload.js'),contextIsolation:true,sandbox:true}});win.loadFile(path.join(__dirname,'renderer','chrome.html'));win.on('resize',layout);win.webContents.once('did-finish-load',()=>showTab(makeTab()))}app.whenReady().then(()=>{loadStore();createWindow()});app.on('window-all-closed',()=>{if(process.platform!=='darwin')app.quit()});app.on('activate',()=>{if(!BrowserWindow.getAllWindows().length)createWindow()});ipcMain.handle('new-tab',(_,u,p)=>{const id=makeTab(u||HOME,!!p);showTab(id);return id});ipcMain.on('switch-tab',(_,id)=>showTab(Number(id)));ipcMain.on('close-tab',(_,id)=>closeTab(Number(id)));ipcMain.on('navigate',(_,v)=>{const t=tabs.get(activeId);if(!t)return;let u=String(v||'').trim();if(!u)return;if(!/^https?:\/\//i.test(u))u=/^[\w.-]+\.[a-z]{2,}(\/.*)?$/i.test(u)?'https://'+u:'https://www.google.com/search?q='+encodeURIComponent(u);t.view.webContents.loadURL(u)});ipcMain.on('back',()=>{const t=tabs.get(activeId);if(t?.view.webContents.canGoBack())t.view.webContents.goBack()});ipcMain.on('forward',()=>{const t=tabs.get(activeId);if(t?.view.webContents.canGoForward())t.view.webContents.goForward()});ipcMain.on('reload',()=>tabs.get(activeId)?.view.webContents.reload());ipcMain.handle('get-tabs',()=>[...tabs.values()].map(t=>({id:t.id,title:(t.privateMode?'🕶️ ':'')+(t.view.webContents.getTitle()||'New Tab'),url:t.view.webContents.getURL(),privateMode:t.privateMode,active:t.id===activeId})));ipcMain.handle('install-extension',async()=>{const r=await dialog.showOpenDialog(win,{properties:['openDirectory'],title:'Select unpacked Chrome extension folder'});if(r.canceled)return{ok:false};try{const e=await session.defaultSession.loadExtension(r.filePaths[0],{allowFileAccess:true});store.extensions=[...(store.extensions||[]).filter(x=>x.id!==e.id),{id:e.id,name:e.name,path:r.filePaths[0]}];saveStore();return{ok:true,name:e.name,id:e.id}}catch(e){return{ok:false,error:e.message}}});ipcMain.handle('get-bookmarks',()=>store.bookmarks);ipcMain.handle('add-bookmark',(_,x)=>{if(!x?.url)return false;store.bookmarks=[x,...store.bookmarks.filter(b=>b.url!==x.url)];saveStore();return true});ipcMain.handle('remove-bookmark',(_,u)=>{store.bookmarks=store.bookmarks.filter(b=>b.url!==u);saveStore();return true});ipcMain.handle('get-history',()=>store.history);ipcMain.handle('clear-history',()=>{store.history=[];saveStore();return true});ipcMain.handle('get-extensions',()=>store.extensions||[]);ipcMain.handle('get-downloads',()=>store.downloads||[]);ipcMain.handle('clear-downloads',()=>{store.downloads=[];saveStore();return true});ipcMain.handle('active-state',()=>{const t=tabs.get(activeId);return t?{id:activeId,url:t.view.webContents.getURL(),title:t.view.webContents.getTitle()}:null});